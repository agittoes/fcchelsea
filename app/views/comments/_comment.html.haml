%a{:name=>"comment-#{comment.id}"}
.row-fluid.comment{:id=>"comment-#{comment.id}"}
  .span4
    =render :layout => @user do
      .date
        =time_ago_in_words(comment.created_at)
        ago
  .span8
    .text
      =comment.text

.row-fluid
  .span4.sidebar
    .last-posts
      %h3
        ბოლო სიახლეები
      -posts_for_side_bar.each do |post|
        .post
          .title
            =link_to post.title, post
          .content
            .image
              =link_to post do
                =image_tag post.image.url(:sidebar)
            .text
              =post.intro
              %br/
              =link_to '(სრულიად წაკითხვა)', post
            .clearfix
          .date
            =format_date(post.created_at)

  .span8.content
    =render 'main/carousel'
    %Results
    =render 'main/table'